# GPT-6 Astra

Use Core. Add these deltas only when relevant:

- In a real multi-agent workflow, explicitly define when independent parallel work should be delegated; Astra may otherwise delegate less often than the workflow benefits from.
- When the desired output should avoid Astra's detailed, Markdown-heavy default, state the required prose/structure directly instead of relying on generic anti-formatting rules.
