# GPT-5.6

Use Core. Add this delta when relevant:

- GPT-5.6 is relatively concise by default. When optimizing an existing prompt, remove broad brevity instructions such as “be concise” or “keep it short” unless the requested output shape actually needs them; specify the information that must be preserved instead.
