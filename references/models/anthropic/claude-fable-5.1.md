# Claude Fable 5.1

Use Core. Add these deltas only when relevant:

- When readability or structure matters, explicitly request suitable paragraphing and formatting; Fable 5.1 can produce denser prose, and legacy anti-Markdown rules may suppress useful structure.
- For small file changes, prefer targeted edits over whole-file rewrites when both produce the same result.
