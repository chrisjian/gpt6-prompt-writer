# GPT‑6 官方依据与规则映射

核验日期：2026-09-07。目标：GPT‑6 Astra / `gpt-6-astra`。以下是机制摘要与本技能的原创操作规则，不是官方模板原文或性能认证。

## 来源边界

| 编号 | 实际读取的官方页面 | 用途 |
| --- | --- | --- |
| S1 | [Using GPT‑6 Astra](https://developers.openai.com/api/docs/guides/latest-model) 及 [模型专属指南](https://developers.openai.com/api/docs/guides/latest-model/gpt-6-astra.md) | 五类提示词倾向、模型 ID、迁移兼容限制 |
| S2 | [Message roles and instruction following](https://developers.openai.com/api/docs/guides/prompt-engineering#message-roles-and-instruction-following) | 稳定指令与用户输入、消息权限、逐轮 instructions |
| S3 | [Reasoning effort](https://developers.openai.com/api/docs/guides/reasoning#reasoning-effort) | 推理档位是 API 控制，需要按模型与评测选取 |
| S4 | [Structured model outputs](https://developers.openai.com/api/docs/guides/structured-outputs) | JSON 与 schema 的区别、受支持 schema、拒绝与不完整结果 |

官方指南是模型行为建议，不是每次调用的行为保证。未检查用户账户可用性、未调用付费 API、未验证延迟或成功率。本技能不收录价格、上下文上限或性能排名，因为提示词写作不需要这些易变字段。

## 五类机制如何改变提示词

| 来源观察（S1） | 本技能的转化 | 需要观察的信号 |
| --- | --- | --- |
| GPT‑6 更可能在补充信息能改变结果时发问 | 定义哪些缺项可假设、哪些必须澄清；写清实际交付物，持续完成已授权工作 | 有足够信息时交付产物，关键歧义仍会指出 |
| 对 Skill / AGENTS.md 等上下文指令更敏感 | 审查冲突与暂停来源；区分技能建议和高优先级指令 | 不能把技能中的例行确认误当成新的硬门槛 |
| 可能偏长、偏多格式及重复措辞 | 明确读者、篇幅、段落/列表选择，给一个必要的风格样例 | 符合用户长度和格式，无套话膨胀 |
| 多代理委派可能少于应用预期 | 有已授权的多代理环境时，明确委派对象与分工条件 | 委派独立任务，主代理检查与整合结果 |
| 编码验证可能过度 | 定义必要检查与扩大测试的触发，完成后停止 | 检查相关行为，避免无变化的重复测试 |

其中，最小任务契约、按需模块、默认一个版本和三类反测是本技能的工程设计，不能称为 OpenAI 官方规定。

## 指令优先级的准确解读

S1 建议避免技能指南压过用户明确意图；S2 同时明确开发者消息优先于用户消息。因此，采用“先服从平台系统与开发者约束，再在这些约束内执行用户任务；技能建议用于协助任务”。不能缩写成“用户覆盖一切指令”。

如技能规则实际造成停顿，应明确文件、具体条款和适用理由。此透明要求不等于为每次读文件增加一轮确认。

## 自主性不是扩大授权

把自主性落实成：常规选择可自行决定；已经授权的可逆步骤继续；需要审批的发布等动作先准备可审阅产物；阻塞只影响依赖它的步骤。先前已明确授权的同一动作不重复索取许可。

遇到新的用户补充时，结合上下文更新任务要求，保留仍有效的目标和已有工作。实际 API 的 mid-turn steering、异步工具与配置更新需要应用实现，不由文字提示词启用。

## 来源刷新

1. 用户要求最新或请求真实 API 配置时，检索并打开 S1；确认当前内容仍指向目标模型。
2. 若入口换代，继续查已指定的 GPT‑6 Astra。未核验前保留本快照，不替换成其他模型的限制。
3. 参数、Structured Outputs 和角色行为只读相关 S2–S4 章节，必要时跟进具体模型页。
4. 网络不可用时明确“依据 2026-09-07 快照”，将新参数主张暂缓；普通提示词仍可交付。
5. 来源冲突时优先更具体、当前的模型页；无法确定则标明冲突，不揉成一个无来源结论。

## 本次资产决策

- 决策：`create`，新建 GPT‑6 专用的单流程 Skill。
- 最近邻检索：`prompt-fusion-architect` 已有泛用生成、诊断、压缩与融合流程；`openai-docs` 已有官方资料检索流程。
- 现有资产不足：泛用写作流程未内置 GPT‑6 五类行为校准及本版 API 边界；资料检索技能不负责稳定交付专用提示词包。
- 组合方式：复用官方检索方式，保留泛用技能的任务边界；不复制其全模式路由，不修改或覆盖相邻技能。
- 状态：`provisional`。有一手资料与结构检查，并保留同上下文 dry-run；尚无独立运行证据。
- 回滚条件：误触发普通业务任务、增加无必要追问、丢失旧提示词硬约束、采用不兼容参数。停止使用新规则，保留触发用例与脱敏观察再修订。
